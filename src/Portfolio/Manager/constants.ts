import { SelectableCategory } from "./types";

export const SELECTABLE_CATEGORIES: SelectableCategory[] = [
  "sector",
  "industry",
  "country",
];
export const DEFAULT_SELECTED_CATEGORY: SelectableCategory = "sector";
export const DEFAULT_SELECTED_CURRENCY = "inr";
